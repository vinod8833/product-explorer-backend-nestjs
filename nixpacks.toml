[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = ['npm ci', 'cd backend && npm ci']

[phases.build]
cmds = ['cd backend && npm run build']

[start]
cmd = 'cd backend && npm run start:prod'